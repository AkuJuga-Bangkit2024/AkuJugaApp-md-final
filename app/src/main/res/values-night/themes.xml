<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.AkuJuga" parent="Theme.Material3.DayNight">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/gray2</item>
        <item name="colorPrimaryVariant">@color/gray2</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnBackground">@color/white</item>
        <item name="android:windowBackground">@color/gray2</item>
        <item name="android:colorBackground">@color/light_green</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>